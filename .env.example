# Environment Configuration for Dungeon Crawler API
# Copy this file to .env.local and fill in your actual values

# ============================================================================
# SUPABASE CONFIGURATION
# ============================================================================

# Your Supabase Project URL
# Find this in your Supabase project settings under "API" section
# Example: https://xyzcompany.supabase.co
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Your Supabase Anon/Public Key
# Find this in your Supabase project settings under "API" section
# This key is safe to use in the browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Your Supabase Service Role Key (KEEP SECRET!)
# Find this in your Supabase project settings under "API" section
# This key has admin privileges and should NEVER be exposed to the client
# Only use this for server-side operations
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# Optional: Direct database connection string for advanced operations
# Find this in your Supabase project settings under "Database" -> "Connection string"
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.xyzcompany.supabase.co:5432/postgres
# DATABASE_URL=your_database_connection_string

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# Storage mode: 'supabase' or 'file' (default: 'supabase')
# Set to 'file' to use the legacy JSON file storage
# Set to 'supabase' to use Supabase database
STORAGE_MODE=supabase

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. Create a Supabase account at https://supabase.com
# 2. Create a new project
# 3. Go to Project Settings > API to find your URL and keys
# 4. Copy this file to .env.local
# 5. Replace the placeholder values with your actual Supabase credentials
# 6. Run the SQL migrations in supabase/migrations/ folder:
#    - Go to SQL Editor in your Supabase dashboard
#    - Copy and paste the contents of 001_initial_schema.sql
#    - Click "Run" to execute
#    - Optionally run 002_seed_data.sql for test data
# 7. Restart your Next.js development server
#
# For production deployment on Vercel:
# - Add these environment variables in your Vercel project settings
# - Make sure to keep SUPABASE_SERVICE_ROLE_KEY secret (not in NEXT_PUBLIC_)
